#include<bits/stdc++.h>
#define ll long long
using namespace std;
int main(){
	int t;
	cin>>t;
	while(t--){
		ll n,m,w,h;
		cin>>n>>m>>w>>h;
		string s;
		cin>>s;
		vector<pair<ll,ll> > v(m);
		for(ll i=0;i<m;i++){
			cin>>v[i].first>>v[i].second;
		}
	}
}